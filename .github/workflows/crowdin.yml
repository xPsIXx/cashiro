name: Crowdin Sync

# Play Store metadata translations. Cashiro has no Crowdin account, so this
# does not run on push. Leave it here for later; trigger manually only after
# adding CROWDIN_PROJECT_ID and CROWDIN_PERSONAL_TOKEN repository secrets.
on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  crowdin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Crowdin push sources & download translations
        uses: crowdin/github-action@v2
        with:
          upload_sources: true
          upload_translations: false
          download_translations: true
          skip_untranslated_files: true
          localization_branch_name: l10n/crowdin
          create_pull_request: true
          pull_request_title: "i18n: new Play Store metadata translations"
          pull_request_body: "Automated pull of completed store-listing translations from Crowdin."
          pull_request_base_branch_name: main
          commit_message: "i18n: update Play Store metadata translations"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
