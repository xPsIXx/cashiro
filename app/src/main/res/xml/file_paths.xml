<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache directory for temporary export files -->
    <cache-path 
        name="exports" 
        path="exports/" />
    
    <!-- Cache directory for backup files -->
    <cache-path
        name="backups"
        path="backups/" />

    <!-- Internal storage for receipt images -->
    <files-path
        name="receipts"
        path="receipts/" />

    <!-- Cache directory for camera temp files -->
    <cache-path
        name="camera_temp"
        path="camera_temp/" />

    <!-- Cache directory for rendered share cards -->
    <cache-path
        name="share_cards"
        path="share_cards/" />
</paths>